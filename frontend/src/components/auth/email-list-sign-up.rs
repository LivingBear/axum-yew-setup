use yew::function_component;

#[function_component(EmailSignUp)]
pub fn email_list_sign_up() -> Html {
    html! {       
        // create html that represents a form that sends a request to api/auth/email-list
        {"Hi from Email list sign up"}
    }
}